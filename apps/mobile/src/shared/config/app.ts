/** Canonical product identity. Used as a namespace for storage keys. */
export const APP_NAME = 'ExpenseTracker'

/** App display name shown in chrome / about. */
export const APP_DISPLAY_NAME = 'Expense Tracker'
