// The aggregated «Прочие» donut/legend entry. The fill is a token VALUE as
// data (painted directly by the SVG stroke) mirroring --color-muted-foreground,
// same as the mobile twin's config; the label itself is i18n copy supplied by
// the caller.

/** Mirrors --color-muted-foreground (light variant), like the mobile config. */
export const OTHER_ENTRY_COLOR = '#6e6b7c'

/** Stable pseudo-id for the aggregated remainder entry. */
export const OTHER_ENTRY_ID = 'other'
