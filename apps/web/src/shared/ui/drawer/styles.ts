// Drawer chrome paddings, defined once so the wrapper (responsive-dialog)
// and one-off drawer bodies (date-field) render the same mobile rhythm.
export const DRAWER_HEADER_PADDING = 'px-6 pb-4 pt-2'
export const DRAWER_SAFE_AREA_BOTTOM = 'pb-[max(1.5rem,env(safe-area-inset-bottom))]'
export const DRAWER_FOOTER_CLASS = `border-t px-6 ${DRAWER_SAFE_AREA_BOTTOM} pt-4`
