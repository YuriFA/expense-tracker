# Repository Audit — 2026-09-01

Full-repo audit: spec-to-code compliance for all 21 capability specs in
`openspec/specs/`, re-verification of the architecture invariants
(`invariants.md` baseline 2026-08-20), and a codebase-improvement sweep.
Findings are recorded canonically in `findings.md` (Rev 3, IDs A16–A20,
B6–B9) and `technical-debt.md`; this file is the evidence report.

**Audited at:** HEAD `d21bebd` (main). **Caveat:** the working tree carried
the in-flight, uncommitted `adjustment-transactions` change (40 modified
files + new migrations) throughout the audit — spec verdicts are anchored at
HEAD; the in-flight state is reported separately (§5). Web/mobile suites
were re-run at HEAD in a temporary worktree to separate transient mid-change
failures from real ones.

## 1. Gate results

| Gate | Where | Result |
|---|---|---|
| `openspec validate --specs` | 21 specs | ✅ all pass (format only — spec↔code compliance has no automation; this audit is the first sweep) |
| `pnpm arch:check` (dep. cruiser, 3 configs, 1086 modules) | local (Node 24) | ✅ 0 violations |
| CI `arch-check` job | GitHub Actions | ❌ **red on every run since its introduction** (2026-08-21): dependency-cruiser 18 rejects the job's Node 20 (`ERROR: Your node version (20.20.2) is not supported`) — `ci.yml` pins `node-version: "20"` in 3 places. Last green CI run of any kind: 2026-08-19 (`fe30c7d`). Finding A17 |
| `pnpm knip` | local | ✅ clean |
| Redocly lint `docs/api/openapi.yaml` | local | ✅ valid |
| `make -C backend gen-check` | HEAD | ✅ (CI `gen-check` job green in the last runs); local run on the live tree is red *by design* while `adjustment-transactions` edits `openapi.yaml` — note `gen-check` regenerates as a side effect |
| Backend `go build` / `go vet` / `go test -race ./...` (testcontainers) | live tree | ✅ all green (incl. the in-flight backend work) |
| Web `type-check`, `oxlint`, `i18n:lint` | live tree | ✅ green |
| Web `steiger src` (FSD) | local | ❌ **2 errors**: cross-slice import `widgets/mobile-shell/ui/MobileTopBar.vue:6` → `widgets/sync-status`; `shared/lib` grouping 17 > 15. Not in CI → invisible (finding A19) |
| Web `test:unit` (Vitest) | **HEAD worktree** | ✅ 85 files / 588 tests (live tree 586/588 — transient in-flight fallout) |
| Mobile `type-check` | HEAD worktree | ✅ (live tree red mid-change: `local-data/sync-data.ts` references removed `manualAdjustment`) |
| Mobile Jest | **HEAD worktree** | ❌ **3 suites / 7 tests fail at HEAD** — `analytics-screen`, `analytics-detail-screen`, `category-cashflow-sheet`. Root cause: date-dependent fixtures (broken at the Sept-1 month boundary; see A18). Not in CI → unnoticed |

## 2. Spec-to-code compliance (21 specs)

Method: every requirement and scenario of each spec traced to code (handlers,
services, repositories, migrations, screens, hooks, unit/e2e tests), with
file:line evidence collected per item. Summary verdicts:

| Spec | Verdict | Notes |
|---|---|---|
| accounts | COMPLIANT | balance server-computed in SQL; deletion guard incl. planned payments; CAS. In-flight delta (manual adjustment removal) not yet landed (§5) |
| transactions | COMPLIANT | idempotent create, safe update, keyset pagination all match. Contract self-contradiction noted (B7): `Idempotency-Key` described as optional ("if present") but `required: true` + middleware 400s on absence (`openapi.yaml:1104-1106` vs `:2072-2078`, `middleware/idempotency.go:56-58`) |
| categories | COMPLIANT | no drift |
| debts | **DRIFT — bug** | `GET /api/debtors` and `GET /api/debt-operations` pass `user.ID` where the service takes `householdID` (`transport/http/debtors.go:17`, `debt_operations.go:17`; every sibling handler uses `currentHouseholdID`). Returns `[]` for every user registered after the household migration (new registrations mint distinct `userID`/`householdID`, `postgres/users.go:28-29`; only the 000005 backfill rows coincide, `000005_household.up.sql:36-40`). e2e misses it: list assertions are `NotContains`-only (`e2e/debts_test.go:129-135`). Finding **A16** |
| planned-payments | COMPLIANT (behavior) / **SPEC-STALE** | spec still says "belongs to exactly one user … another user's … SHALL behave as if it does not exist"; code is household-scoped (sibling specs were reworded; this one wasn't). Finding B8 |
| household | COMPLIANT | all 20 requirements incl. invitations, join code, rebase choice, dissolution; isolation asserted at service + e2e tiers |
| api-hardening | COMPLIANT | **Origin-check CSRF middleware is implemented** (`middleware/origin.go:17-45`, mounted pre-CORS `server.go:62`, 403 `ORIGIN_REJECTED`); registration attempt limiter (`middleware/ratelimit.go:108-185`, 429 `REGISTER_RATE_LIMITED` + Retry-After); health endpoint. The "decided but not implemented" claims live only in stale docs (B6) |
| app-currency | COMPLIANT | RUB-only creation in both apps; contract stays multi-currency-ready |
| app-version | COMPLIANT | API + web report build version. Spec gap: mobile has no runtime version surface at all (B9) |
| operations | COMPLIANT | SMTP mailer (3 TLS modes, log-only fallback), backup sidecar (7 daily + 4 weekly, rclone replication, restorable per runbook). Runtime delivery/backup execution UNVERIFIABLE by design |
| deployment | COMPLIANT | Traefik routing, TLS at gateway, no published ports, manual-only deploys, workstation path, runbook. Live VPS UNVERIFIABLE |
| web-local-data | COMPLIANT | anonymous-first, ownership gate, single-tab Web Locks, persistence — all with Playwright evidence. Wording gap: spec doesn't scope the sync badge to signed-in mode (it's hidden when anonymous) |
| web-screens | COMPLIANT | router matches the inventory plus 5 unenumerated auth/aux routes (spec omission, not drift); dashboard month-scoping sentence internally inconsistent with its own scenario |
| web-theme | COMPLIANT | flash-free startup holds (theme applied before mount); no inline head script — blank pre-app body paints light but no mis-themed content |
| web-locales | COMPLIANT | RU default, EN parity enforced by two gates |
| web-pwa | **DRIFT** | manifest colors stale vs the Warm Paper token palette: `theme_color: "#6366f1"` is the *data-palette* indigo (tokens file says brand-* is "not the UI accent"), `background_color: "#ffffff"` matches no token (light background is `#f6f2ec`; UI primary `#0f766e`). Finding **A20** |
| mobile-forms | COMPLIANT | RHF+Zod everywhere, zero stragglers; money as digit strings through the mapper seam |
| mobile-local-data | COMPLIANT | local DB is the source of truth; no bypassing screen; domain rules enforced locally (reminder firing on-device UNVERIFIABLE, unit-tested) |
| mobile-transaction-edit | COMPLIANT (minor DRIFT) | post-save sheet auto-dismiss is implemented but runtime-flaky (`TODO(sheet-dismiss)` hedged in 4+ Maestro flows) |
| analytics | COMPLIANT | period nav/attribution, breakdown with master toggle, capped donuts, interactive detail donut, integer aggregation |
| sync-protocol | COMPLIANT | all 13 requirements / 24 scenarios verified across backend + `local-data` engine + mobile wiring. Caveats: (1) `AdoptOrphanedID` physically deletes orphaned rows without a tombstone — safe (memberless household, no cursor can read its log) but a documented exception to "every delete appends"; (2) spec tension R1 ("conflict carries server state") vs R12 ("cross-household reveals no state") — code follows R12; carve the exception into R1; (3) "each operation independently" is one DB tx per batch — infra errors abort the batch (retries replay opIds); (4) pull returns current-state rows with change-time versions (documented convergence choice); (5) per-op `last_error` is persisted but not surfaced in mobile UI (already in tech-debt) |

**Totals:** 18 fully compliant, 2 DRIFT (debts listing bug, PWA manifest
colors), 1 minor DRIFT (mobile sheet dismiss), 1 SPEC-STALE
(planned-payments wording), plus 5 spec-quality gaps (B7–B9, web-screens
inventory, web-local-data badge wording).

## 3. Invariant re-verification (vs 2026-08-20 baseline)

Verified in code, not just re-read from docs:

- **Hold with fresh evidence:** #1 (contract gen gates green at HEAD), #2
  (no `float64` in backend money paths — the only two are a metrics ratio in
  `transport/http/sync.go:152-156`; drizzle money columns are `integer`;
  mapper seams used in 13 web / 17 mobile files; dinero carries integer
  minor units), #4 (`writeDomainError` remains the single mapper;
  `RepositoryError` classification in 13 web / 27 mobile files; no
  status-based branching found), #6, #7, #8, #9 (zero `time.Local` in
  non-test backend code), #10, #11 (only sanctioned `fetch` uses outside
  `packages/api`: the web boot health probe `main.ts:71` and the WASM binary
  fetch; tests excluded), #12–#14 (arch:check clean locally), #16, #18
  (still exactly the two registered deviations).
- **Changed status — implemented but docs were stale (#3):** the ADR-0001
  Origin check and the registration rate limit are implemented and e2e-tested
  (`middleware/origin.go`, `middleware/ratelimit.go`,
  `e2e/hardening_test.go:67-105`, landed with `backend-hardening`,
  2026-08-30). invariants.md, findings.md, ADR-0001, and overview.md all
  still said "not implemented" — fixed by this audit (B6 resolved).
- **Live deviation found (#5):** the debts listing bug (A16) is precisely
  the documented caveat in action — "a new unscoped query is caught only if
  a test happens to cover it". The household scoping rule itself is sound;
  two call sites slip `user.ID` into the `householdID` parameter.
- **Web FSD (#15):** Steiger is red locally (A19) — the "grep found no
  violations" evidence in invariants.md was stale.
- **Allowlist stale (#17):** the auth middleware actually depends on
  `HouseholdRepository` (`middleware/auth.go:29`, documented in
  `backend/AGENTS.md`); invariants.md's enumeration omitted it — fixed.
- **Enforcement-meta finding (A17):** the CI `arch-check` job — the
  enforcement behind "Automated: yes (since 2026-08-20)" for #12–#16 — has
  **never been green** (Node 20 vs dependency-cruiser 18, red since
  2026-08-21). The rules hold (local runs on Node 24 are clean), but the CI
  claim was not operational.

## 4. Findings (canonical: `findings.md` Rev 3)

| ID | Class | Priority | Finding |
|---|---|---|---|
| A16 | FIX | P1 | Debts listing household-scoping bug (`debtors.go:17`, `debt_operations.go:17` pass `user.ID`); empty lists for post-migration registrations; add a positive e2e list assertion |
| A17 | FIX | P1 | CI red since 2026-08-21: bump `ci.yml` `node-version` 20 → 22 (3 places); makes `arch-check` operational and un-reds main (14 local commits are also unpushed) |
| A18 | FIX | P2 | Mobile date-dependent test fixtures: `category-cashflow-sheet.test.tsx` `dayThisMonth` clamping collapses days on the 1st–6th of a month; `analytics-screen`/`analytics-detail-screen` hardcode `2026-08-*` vs `currentPeriod('month')` — broke 2026-09-01. Make fixtures month-independent (injected clock) |
| A19 | FIX | P2 | Web FSD: `MobileTopBar.vue:6` imports `@/widgets/sync-status` (cross-slice); `shared/lib` 17 modules > 15. Fix the import; add Steiger (or port the rules to depcruiser web config) to CI |
| A20 | FIX | P3 | PWA manifest colors not token-derived (`site.webmanifest:9-10`); also stale comment `packages/tokens/src/index.css:92` ("dark unwired at runtime") |
| B6 | DOCUMENT | — | Stale docs (resolved by this audit): ADR-0001:66, overview.md CSRF/rate-limit paragraph + vitest counts (71/64 → 85), findings.md registered-deviations list, ADR-0002 status header ("deferred" while household is live), invariants #3/#15/#17 |
| B7 | DOCUMENT | P3 | OpenAPI self-contradiction: `Idempotency-Key` "if present" description vs `required: true` vs middleware 400 — align the description |
| B8 | DOCUMENT | P3 | planned-payments spec still user-scoped in wording; reword to household like its four siblings (spec delta) |
| B9 | DECIDE | P3 | app-version spec gap: mobile has no runtime version surface — decide to add one or record the exclusion |

Improvement backlog beyond findings: frontend CI at all (type-check + unit
suites for web/mobile — A14 remainder; would have caught A18 months of
month-boundary breakage and the mid-flight type skew), `openspec validate`
as a CI step, a top-level `make check` aggregating gates, positive e2e
assertions for listings (pattern behind A16), and the sync-protocol spec
carve-outs from §2 (R1↔R12, orphan-adoption exception).

## 5. In-flight change `adjustment-transactions` (not committed)

State observed 2026-09-01: 10/24 tasks; sections 1 (OpenAPI) and 2 (backend)
done in the working tree (enum added everywhere, `manualAdjustment` fully
removed from contract + backend + migrations `000007`, e2e
`adjustment_test.go` covers every delta scenario); `packages/api` +
`packages/local-data` migrated (including drizzle migration `0004`);
web (section 3) and mobile UI (4.2) pending — the mobile conflict-center
still replays the removed `manualAdjustment` field (dead code until 4.2
lands), and apps' `type-check` is red mid-change as tasks 4.3/5.2
anticipate. Bookkeeping mismatches: task 4.1's work is already done in the
tree but unchecked; no task names the hand-written `packages/api`
domain/http/repositories updates that were needed. The change documents
themselves are internally coherent (deltas match design D1–D7).
