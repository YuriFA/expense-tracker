## 1. Contract (OpenAPI-first)

- [x] 1.1 Extend `docs/api/openapi.yaml`: `Debtor` / `DebtOperation` schemas (+ create/update requests — optional client UUID on create, required `version` on update, immutable `direction`/`kind` documented), paths `/api/debtors`, `/api/debtors/{id}`, `/api/debt-operations` (`?debtorId=` filter), `/api/debt-operations/{id}` with the D3 error codes; `SyncEntity` enum += `debtor` / `debt_operation`; `DebtorSyncData` / `DebtOperationSyncData` oneOf branches
- [x] 1.2 Regenerate everywhere and commit: backend `make gen` (`internal/api/api.gen.go`), `pnpm gen:api` (`packages/api/src/schema.ts`); `npx @redocly/cli lint --config docs/api/redocly.yaml docs/api/openapi.yaml`, `make gen-check`, and the `ts-gen-check` job's local equivalent all green

## 2. Backend: storage and domain

- [x] 2.1 `make migrate-create name=add_debts`: `debtors` + `debt_operations` tables (uuid pk, `user_id` FK cascade, `version`, timestamps, `deleted_at`, `note TEXT NOT NULL DEFAULT ''` on both, CHECK `direction`/`kind`/`amount > 0`, partial unique `(user_id, name) WHERE deleted_at IS NULL` mirroring the categories pattern, indexes `(user_id, occurred_at)` and `(user_id, debtor_id)`); extend `change_log.entity` + `applied_operations.entity` CHECKs via `NOT VALID` + `VALIDATE CONSTRAINT` (design D4)
- [x] 2.2 `internal/domain/debtor.go` + `debt_operation.go` (entities, params with pointer "nil = keep" fields for name/note, `FullState`) + sentinels in `errors.go`; extend `domain/sync.go` entity constants
- [x] 2.3 `internal/repository/queries/debtors.sql` + `debt_operations.sql` (live-filtered CRUD, `GetAny` incl. tombstones for conflict classification, `HasLiveDebtOperationsForDebtor` counting live operations only, `DebtorNameTaken` live-name pre-check for the sync path, `SyncReplace*` CAS on base version, `Sync*ByIDs` for pull, `DeleteTombstonedDebtOperationsBefore` for retention) → `make gen` (sqlc)
- [x] 2.4 `internal/repository/interfaces.go` (`DebtorRepository`, `DebtOperationRepository`, `SyncTx` + `TombstoneRetention` extensions) + `internal/repository/postgres/` impls via `withinLockedTx` + `appendChangeLog`, with 0-row write classification (tombstoned/missing → not-found; live version mismatch → version-conflict)

## 3. Backend: service, transport, sync

- [x] 3.1 `internal/service/debtor.go` + `debt_operation.go`: user scoping, shape validation (positive amount, known direction/kind), debtor-ref existence/ownership check against LIVE debtors, unique-name and live-only in-use guards, version CAS semantics, note absent=keep / null rejected / `""` clears (design D3); extend `internal/service/fakes/fakes.go`
- [x] 3.2 Sync participation mirroring the generic machinery (design D8): `applySyncOperation` dispatch for both entities; delete idempotent + delete-wins (never a version conflict), in-use as per-item error; create-vs-existing-id → `SYNC_ALREADY_EXISTS` conflict with server state; `DebtorNameTaken` pre-check → per-item `DEBTOR_ALREADY_EXISTS` error; debtor-ref validation → per-item `DEBT_OPERATION_DEBTOR_NOT_FOUND` error; only APPLIED results recorded in `applied_operations`; pull-state fetchers (`fetchPullStates` / `pullStateOf`); retention ordering debt_operations → debtors (design D4)
- [x] 3.3 Transport: `internal/transport/http/debts.go` + `debt_operations.go` strict handlers, `convert.go` mappers, `errormap.go` rows for every D3 code, `Server` struct + `NewServer` param + `main.go` wiring
- [x] 3.4 Tests: service tests over fakes (shape, uniqueness, in-use, version conflicts, direction/kind immutability), repository tests (testcontainers; CAS + change-log atomicity + CHECK migration), e2e (REST CRUD roundtrip + sync push/pull convergence incl. a debtor-in-use delete and an over-repayment balance) plus the edge cases of D8: debtor deletable once all its operations are tombstoned; concurrent delete/update → delete-wins; REST update/delete of a tombstoned record → not-found; sync delete of an already-tombstoned record → applied with current version; opId replay → stored applied result and no second record; different opId + same entity id → `SYNC_ALREADY_EXISTS`; pushed operation for a server-deleted debtor → per-item `DEBT_OPERATION_DEBTOR_NOT_FOUND` error without aborting the batch

## 4. packages/api

- [x] 4.1 `src/domain/debtor.ts` + `debt_operation.ts` (models + normalize guards), `src/repositories/` contracts (update payloads carry `version`; optional note fields with absent=keep / `""` clears / never `null`, design D3), `src/http/` mappers (absent fields omitted from JSON, empty string always sent); export from the barrel; extend `api-errors.ts` code sets (`IN_USE_CODES` += `DEBTOR_IN_USE`, `VERSION_CONFLICT_CODES` += both conflict codes, not-found sets)
- [x] 4.2 Gates: `pnpm type-check` + lint in `packages/api`, web `type-check` still green (additive schema), `pnpm knip` clean

## 5. Mobile: data layer

- [x] 5.1 `shared/lib/db/schema.ts`: `debtors` + `debt_operations` tables (sync columns, text `direction`/`kind`, integer money, UUID ids) + extend the `SyncEntity` union; `pnpm db:generate` (drizzle migration + inlined `migrations.generated.ts`)
- [x] 5.2 Sync plumbing: `sync-data.ts` (row/payload/sync-data mappers for both entities), `sync-engine.ts` (`updateEntityRow`, `insertFromChange`, plus the unknown-entity skip-and-advance default with logging — design D5), `outbox.ts`, `conflicts.ts`, `sync-meta.ts` `wipeLocalData`
- [x] 5.3 `entities/debt` slice: local repository (atomic row + outbox, local debtor in-use guard counting only live operations — mirror the category guard's `isNull(deletedAt)` check, unborn-wipe vs tombstone with delete ops carrying `baseVersion = serverVersion`, apiCode-correct errors), repository provider, hooks (`useDebtors`, `useDebtOperations`, create/update/delete mutations with cache invalidation), barrel; add `'debt'` to `ENTITY_SLICES` in `.dependency-cruiser.mobile.cjs`; compose the provider in `_layout.tsx`
- [x] 5.4 Tests: repository suite over `test-database` (CRUD, live-only in-use guard — debtor deletable once all operations are tombstoned, outbox atomicity), sync roundtrip test for both entities, sync-engine tests: feeding a foreign/unknown entity kind (D5) and an error result for an operation whose debtor is deleted → op keeps `lastError`, stays queued, no `sync_conflicts` row; selector tests for balance derivation (both-directions, over-repayment, zero-balance)

## 6. Mobile: screen, quick action, goals removal

- [x] 6.1 `pages/debts/model/`: RU copy record (`kind.ts` pattern, `TODO(i18n)` markers) + selectors (per-direction balances and totals, day-grouped history, settled partition, balance-descending sort — integer math, `formatAmount` at the edges)
- [x] 6.2 Route `src/app/debts.tsx` + `<Stack.Screen name="debts" />`; `ui/debts-screen.tsx`: `Screen` + `ScreenHeader «Долги»` + `ScreenScrollView`, dual-total summary card (no period nav), МНЕ ДОЛЖНЫ / Я ДОЛЖЕН sections with `DebtorRow` (initials avatar on palette color hashed from id, name, balance, chevron), hidden settled debtors with a «Показать рассчитавшихся (N)» reveal row per section, empty states; the overview loads ALL operations through one `useDebtOperations()` query and derives every figure in selectors — no per-debtor repository calls (design D7 perf invariant)
- [x] 6.3 Debtor history sheet: remaining balance for the direction, day-grouped operations labeled «Долг» / «Списание» with sign-colored amounts, row tap → edit-operation sheet, debtor edit affordance, «Новое списание» footer CTA
- [x] 6.4 Operation form (new + edit variants) per `docs/conventions/forms.md`: RHF + Zod (`model/schema.ts`), Долг ↔ Списание kind switch, direction segmented control (fixed when opened from a debtor sheet), debtor picker sheet, shared amount keypad, date picker, note, over-repayment warning (warn, don't block), root-error via `getRepositoryErrorText`, CAS `version` on edit
- [x] 6.5 Debtor create/edit sheet (name, note) with rename-conflict and empty-name error mapping; delete with confirm alert
- [x] 6.6 Quick actions row: replace the goals entry with `id: 'debts'`, label «Долги», `hand-coins` icon (add the glyph if missing, else the closest coins glyph), enabled, `home-quick-debts` testID; delete `src/app/goals.tsx`, `pages/goals/`, and the `goals` `Stack.Screen` entry (design D7)

## 7. Gates, e2e, docs

- [x] 7.1 Jest suites: debts screen (totals, sections, reveal row, empty states), history sheet, operation + debtor forms (validation, over-repayment warning, error mapping), plus the performance pin — render the overview with several debtors against a mocked repository and assert exactly one operations read (design D7)
- [x] 7.2 Maestro flow `apps/mobile/.maestro/flows/14-debts.yaml`: home → «Долги» chip → totals assert → debtor sheet → «Новое списание» → remaining balance updates → settled debtor hidden; assert the derived balance figures themselves (summary totals and row amounts as text), not just element presence
- [x] 7.3 E2E prerequisite — offline infrastructure (none exists today): a dev-build-only network gate that makes the sync transport fail, surfaced as an in-app toggle with a stable testID (e.g. `settings-dev-offline-toggle`), following existing dev-build accommodations (`with-dev-menu-fab.js`); and per-state testIDs on `sync-status-badge` (e.g. `sync-status-synced` / `sync-status-pending`) so flows can assert sync state by testID instead of label text. Maestro's `runScript` cannot toggle the simulator network, and loopback is unaffected by device Wi-Fi — the in-app gate is the only reliable mid-flow mechanism
- [x] 7.4 New Maestro flow `apps/mobile/.maestro/flows/15-debts-offline.yaml` (separate file; existing flows untouched), blocked on 7.3: sign in (pattern of `09-sync-signin.yaml`) → create debtor + debt online → toggle offline → record a repayment offline → assert the new derived balance offline → restart the app (re-run `_launch.yaml`; killApp + deep link, never `clearState` — the local DB persists) → assert the balance survived the restart → toggle online → force sync (tap `sync-status-badge` or `settings-sync-now-button`; AppState foreground also triggers a run) → `extendedWaitUntil` the synced-state testID → assert the repayment is still reflected. No coordinate hacks or simulator-level network commands
- [x] 7.5 Full gates: mobile `pnpm type-check && pnpm lint && pnpm format && pnpm test && pnpm test:e2e`; root `pnpm arch:check` and `pnpm knip`; backend `go test -race ./...` (or `-short` without Docker) + `make gen-check`
- [x] 7.6 Docs: `docs/product/mobile-home.md` quick-actions section (Счета / Доходы / Долги; debts removed from "explicitly excluded"; goals deferred without a tile); sweep `docs/assumptions.md` for goals references
- [x] 7.7 `openspec validate add-debts --strict` passes
