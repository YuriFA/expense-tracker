## 1. Contract (OpenAPI-first)

- [x] 1.1 Extend `docs/api/openapi.yaml`: `PlannedPayment` schema (+ create/update requests — optional client UUID on create, required `version` on update, immutable `type` documented; `name` / `note` optional non-nullable strings defaulting to `""`; `next_due` / `anchor_date` as `format: date`; `regularity` / `confirm_mode` / `reminder` enums), paths `/api/planned-payments` (`?type=` filter) and `/api/planned-payments/{id}` with the D3 error codes; `SyncEntity` enum += `planned_payment`; `PlannedPaymentSyncData` oneOf branch in all three sync-data lists
- [x] 1.2 Regenerate everywhere and commit: backend `make gen` (`internal/api/api.gen.go`), `pnpm gen:api` (`packages/api/src/schema.ts`); `npx @redocly/cli lint --config docs/api/redocly.yaml docs/api/openapi.yaml`, `make gen-check`, and the `ts-gen-check` job's local equivalent all green

## 2. Backend: storage and domain

- [x] 2.1 `make migrate-create name=add_planned_payments`: `planned_payments` table (uuid pk, `user_id` FK cascade, `version`, timestamps, `deleted_at`, `type` / `regularity` / `confirm_mode` / `reminder` CHECKs, `amount BIGINT CHECK (amount > 0)`, `name` / `note TEXT NOT NULL DEFAULT ''`, NO name unique index, `next_due DATE NOT NULL` / `anchor_date DATE NOT NULL`; partial indexes `(user_id, next_due)`, `(user_id, account_id)`, `(user_id, category_id)` all `WHERE deleted_at IS NULL`); extend `change_log.entity` + `applied_operations.entity` CHECKs via `NOT VALID` + `VALIDATE CONSTRAINT` (design D4)
- [x] 2.2 `internal/domain/planned_payment.go` (entity; `CreatePlannedPaymentParams` with optional client id; `UpdatePlannedPaymentParams` with pointer "nil = keep" fields where a `next_due` change also sets `anchor_date`; pure `AdvanceNextDue(nextDue, anchor, regularity)` implementing the D2 clamp-and-recover rules) + sentinels in `errors.go`; extend `domain/sync.go` entity constants
- [x] 2.3 `internal/repository/queries/planned_payments.sql` (live-filtered CRUD, `GetPlannedPaymentAny` incl. tombstones for conflict classification, `HasLivePlannedPaymentsForAccount` / `HasLivePlannedPaymentsForCategory` counting live rows only, `SyncReplacePlannedPayment` CAS on base version, `SyncPlannedPaymentsByIDs` for pull, `DueAutoPlannedPayments` live `auto` plans with `next_due <= today` for the job, `DeleteTombstonedPlannedPaymentsBefore` for retention) → `make gen` (sqlc)
- [x] 2.4 `internal/repository/interfaces.go` (`PlannedPaymentRepository`, `SyncTx` + retention extensions) + `internal/repository/postgres/planned_payments.go` via `withinLockedTx` + `appendChangeLog`, with 0-row write classification (tombstoned/missing → not-found; live version mismatch → version-conflict)

## 3. Backend: service, transport, sync, auto-confirm job

- [x] 3.1 `internal/service/planned_payment.go`: user scoping, shape validation (positive amount, known enums, LIVE account ref, LIVE type-matched category ref, past `next_due` allowed), no name-uniqueness rules, version CAS, `type` immutability, no-op update rejection, note/name absent=keep / null rejected / `""` clears (design D3); extend the account and category delete services with the live-plans in-use guard (accounts / categories spec deltas); extend `internal/service/fakes/fakes.go`
- [x] 3.2 Sync participation mirroring the generic machinery (design D8): `applySyncOperation` dispatch for `planned_payment`; delete idempotent + delete-wins (never a version conflict); create-vs-existing-id → `SYNC_ALREADY_EXISTS` conflict with server state; account/category ref validation → per-item `PLANNED_PAYMENT_ACCOUNT_NOT_FOUND` / `PLANNED_PAYMENT_CATEGORY_NOT_FOUND` errors without aborting the batch; only APPLIED results recorded in `applied_operations`; pull-state fetchers (`fetchPlannedPaymentPullStates` / `pullStateOf`); retention ordering gains planned payments after transactions and before categories/accounts (design D4)
- [x] 3.3 Auto-confirm job `internal/jobs/plannedconfirm` (design D5): hourly ticker wired in `main.go` like the retention job; per-user `WithinUserTx`, for each due `auto` plan loop `while next_due <= today(UTC)`: create the transaction (plan's type/account/category/amount, `occurred_at = <scheduled date> 12:00:00Z`, note = plan name) + advance `next_due` + both change-log entries in the same tx; idempotent by construction (rerun finds nothing due)
- [x] 3.4 Transport: `internal/transport/http/planned_payments.go` strict handlers, `convert.go` mappers, `errormap.go` rows for every D3 code, `Server` struct + `NewServer` param + `main.go` wiring
- [x] 3.5 Tests: service tests over fakes (shape, type-mismatched category, ref guards, version conflicts, type immutability, no-op update); repository tests (testcontainers; CAS + change-log atomicity + CHECK migration + clamp-recover `AdvanceNextDue` vectors shared with mobile); e2e: REST CRUD roundtrip, sync push/pull convergence, account/category delete blocked by a live plan (409 in-use), pushed plan referencing a server-deleted category → per-item error while the rest of the batch applies, job tests (due plan → one transaction + advancement; three months behind → three transactions on dated `occurred_at`; rerun → no-op; manual plan never auto-executed)

## 4. packages/api

- [ ] 4.1 `src/domain/planned_payment.ts` (model + normalize guard), `src/repositories/planned-payment.ts` contract (update payload carries `version`; `name` / `note` absent=keep / `""` clears / never `null`), `src/http/` mappers (absent fields omitted from JSON, empty string always sent); export from the barrel; extend `api-errors.ts` code sets (`VERSION_CONFLICT_CODES` += `PLANNED_PAYMENT_VERSION_CONFLICT`, not-found sets += the new codes)
- [ ] 4.2 Gates: `pnpm type-check` + lint in `packages/api`, web `type-check` still green (additive schema), `pnpm knip` clean

## 5. Mobile: data layer

- [ ] 5.1 `shared/lib/db/schema.ts`: `planned_payments` table (mandatory sync columns, `next_due` / `anchor_date` ISO text, text enums, integer money, UUID id) + extend the `SyncEntity` union; `pnpm db:generate` (drizzle migration + inlined `migrations.generated.ts`)
- [ ] 5.2 Sync plumbing: `sync-data.ts` (row/payload/sync-data mappers), `sync-engine.ts` (`updateEntityRow`, `insertFromChange`; add `planned_payment` to the unknown-kind skip-and-advance test list — design D8), `outbox.ts`, `conflicts.ts`, `sync-meta.ts` `wipeLocalData`
- [ ] 5.3 `entities/planned-payment` slice: `model/recurrence.ts` (TS twin of `AdvanceNextDue` + the monthly-normalization selector, integer math, pinned by the shared vectors), local repository (atomic row + outbox, unborn-wipe vs tombstone with delete ops carrying `baseVersion = serverVersion`, apiCode-correct errors, no name checks), repository provider, hooks (`usePlannedPayments` — ONE query, type split in selectors — plus create/update/delete mutations with cache invalidation), barrel; add `'planned-payment'` to `ENTITY_SLICES` in `.dependency-cruiser.mobile.cjs`; compose the provider in `_layout.tsx`
- [ ] 5.4 Manual-confirm composite in the local repository (design D6): one `db.transaction` inserting the transaction row + `enqueueOperation('transaction', …)` AND updating the plan row (`next_due` advanced, `version + 1`) + `enqueueOperation('planned_payment', …)`; exposed as a `confirmPlannedPayment` repository method used by the screen
- [ ] 5.5 Account/category local delete guards: the existing account and category local repositories also count live local planned payments (mirror of the transactions check, design D6)
- [ ] 5.6 Tests: repository suite over `test-database` (CRUD, outbox atomicity, confirm-composite atomicity — a failed plan update rolls back the transaction insert), sync roundtrip test for the entity, sync-engine test (pushed plan erroring with `PLANNED_PAYMENT_CATEGORY_NOT_FOUND` keeps `lastError`, stays queued, no `sync_conflicts` row), recurrence + normalization selector tests (599 monthly + 6000 yearly → 1099,00 ₽/мес; overdue detection; name||category titles)

## 6. Mobile: screen, sheets, forms

- [ ] 6.1 `pages/plans/model/`: `kind.ts` RU copy record (card titles + descriptions, «Добавить расход/доход», regularity / confirm-mode / reminder labels, «Счёт списания» / «Счёт зачисления», `TODO(i18n)` markers), `schema.ts` (Zod form schema: digit-string amount refined via `parseMajorUnitsToMinor`, optional name/note, required account/category, past dates allowed; defaults `monthly` / `manual` / `off`), `selectors.ts` (monthly normalization, next-due sort, overdue flag, row titles)
- [ ] 6.2 `ui/plans-screen.tsx` replaces the `ScreenPlaceholder` (route `(tabs)/plans.tsx` untouched): two cards («Расходы» / «Доходы») each showing live count + normalized `X ₽/мес`; the page owns ALL sheet refs and the `{type, session}` creation context (invariant #15); testIDs `plans-card-expense` / `plans-card-income`
- [ ] 6.3 Per-type list sheet: flat rows sorted by `next_due` ascending (overdue first with a «Просрочен» badge), row = title (`name || category.name`), amount, regularity label, next-due date; bottom «Добавить расход/доход» button; manual plans carry a row-level confirm affordance (auto plans do not — the job owns them)
- [ ] 6.4 Add/edit form sheet per `docs/conventions/forms.md` (design D7): RHF + `FormProvider`, type fixed from the card as the sheet title, keypad amount, optional name, `AccountPickerSheet` / `CategoryPickerSheet` (type-filtered), `DatePickerSheet` via the `DateButton` idiom (past dates allowed), `SegmentedSwitch` ×3 (regularity, confirm mode, reminder), note via the action toolbar, circular submit, root-error via `getRepositoryErrorText`, CAS `version` + delete on edit
- [ ] 6.5 Confirm sheet: static account/category context rows, editable keypad amount (prefilled from the plan), editable date (default the occurrence date), optional note, submit → the 5.4 composite; on success dismiss + invalidate plan/transaction queries
- [ ] 6.6 Jest suites: screen (card figures incl. the normalization example, empty states), list sheet (sort, overdue badge, unnamed-plan titles, confirm affordance on manual rows only), add/edit form (validation, defaults, past date accepted, error mapping), confirm sheet (prefill, amount edit, submit composite, error keeps values)

## 7. Mobile: reminders

- [ ] 7.1 Add `expo-notifications` + app config (Android 13+ `POST_NOTIFICATIONS`, iOS usage string; no push entitlement — local only); `entities/planned-payment/model/reminders.ts`: `reschedule(plans)` cancels the `plan-reminder-*` id prefix and schedules per live plan with `reminder != 'off'` at 10:00 device-local on `next_due` (previous day for `day_before`), skipping past dates; copy per confirm mode («Сегодня спишется/зачислится X» vs «Подтверди платёж X», title «Планы»)
- [ ] 7.2 Permission flow: the form calls `requestPermissionsAsync` when the user first switches reminder away from `off`; denial is silent (setting stored + synced, scheduler no-ops)
- [ ] 7.3 Driver effect: subscribe to the plans + categories query data in the plans data layer and call `reschedule` on data identity change — covers local mutations and pull-driven invalidation (design D9)
- [ ] 7.4 Tests (Jest-mocked notifications module): `day_before` / `on_day` trigger dates, past-date skip, delete cancels, idempotent reschedule, permission-denied no-op

## 8. Gates, e2e, docs

- [ ] 8.1 Maestro flow `apps/mobile/.maestro/flows/16-plans.yaml`: «Планы» tab → «Расходы» card → add a monthly plan (amount, account, category, date) → assert the card count and the normalized monthly total as text → row confirm (edited amount) → assert the plan advanced (next-due text) and the plan total unchanged
- [ ] 8.2 Maestro flow `apps/mobile/.maestro/flows/17-plans-offline.yaml` (offline-gate infra from the debts change): sign in → create plan online → toggle offline → create another plan + confirm one manually → restart the app (killApp + deep link, never `clearState`) → figures survived → toggle online → force sync → synced-state testID → figures match; no coordinate hacks
- [ ] 8.3 Full gates: mobile `pnpm type-check && pnpm lint && pnpm format && pnpm test && pnpm test:e2e`; root `pnpm arch:check` and `pnpm knip`; backend `go test -race ./...` (or `-short` without Docker) + `make gen-check`
- [ ] 8.4 Docs: sweep `docs/` (product notes, assumptions, `.maestro/flows/02-tab-navigation.yaml` hints) for the «Планы» placeholder wording and update to the real screen; no AGENTS.md rule changes expected
- [ ] 8.5 `openspec validate add-planned-payments --strict` passes
